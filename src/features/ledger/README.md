# Ledger Feature

Authenticated ledger presentation and feature orchestration belong here.

Use this feature for dashboard summaries, balance views, settlement views, debt
details, and activity timelines. Financial calculations remain in `src/domain`;
authenticated reads and writes remain in `src/server`.
