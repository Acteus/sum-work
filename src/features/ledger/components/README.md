# Ledger Components

Authenticated ledger presentation components live here.

Components may render dashboard summaries, group previews, balance panels, debt
status cards, and activity timelines. They may consume domain functions and
documented server data passed into them, but must not query Supabase directly or
own financial rules.
