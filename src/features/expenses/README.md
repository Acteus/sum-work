# Expenses Feature

The shared-expense product area lives here.

Presentation components belong in `components/`. Future feature-local hooks or
view models may be added beside that directory when they are specific to
expenses. Financial calculations remain in `src/domain`; persistence remains in
`src/server`.
