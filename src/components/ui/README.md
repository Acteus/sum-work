# Shared UI Primitives

Place reusable, feature-neutral presentation primitives here.

Examples include buttons, fields, dialogs, status badges, and panel shells.
Feature-specific expense and member workflows belong under `src/features`.

Shared primitives must not query Supabase or contain financial business rules.
