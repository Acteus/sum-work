# Supabase Utilities

Low-level Supabase browser, server, environment, and proxy helpers live here.

These modules create infrastructure clients and maintain sessions. They do not
contain application queries, mutations, authorization policy, or domain rules;
those belong in `src/server` and `src/domain`.
