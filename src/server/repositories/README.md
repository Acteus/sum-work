# Repositories

Supabase persistence adapters live here.

Repositories map database records to domain types and support atomic workflows.
They must not define financial policy or trust user-supplied actor IDs.
