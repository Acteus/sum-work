# Server Validation

Input schemas, parsing, and server-side validation helpers live here.

Validation should reject malformed identifiers, invalid centavo amounts,
unsupported state transitions, and values outside configured financial limits.
